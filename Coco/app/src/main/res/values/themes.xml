<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Coco" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Coco" parent="Base.Theme.Coco" />

    <style name="Theme.Coco.Splash" parent="Base.Theme.Coco">
        <item name="android:windowSplashScreenBackground" tools:ignore="NewApi">@color/red</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:ignore="NewApi">@drawable/graph</item>

    </style>
</resources>